root: ./docs/

structure:
  readme: README.md
  summary: SUMMARY.md

# Notes:
# - GitBook publishes only the pages listed in docs/SUMMARY.md.
# - docs/internal/ holds operator/engineer runbooks (smoke test, analytics
#   setup, annual statutory-rate update). It is intentionally NOT in SUMMARY.md,
#   so GitBook will not publish it.
